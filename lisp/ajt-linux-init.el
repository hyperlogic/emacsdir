
;; GNU Linux specific configuration

;; for Ubuntu 200% scaling
(set-face-attribute 'default nil :family "JetBrains MonoNL" :height 95 :weight 'light)

;; NOTE: use vterm-copy-mode to move the cursor like shell
;(if (and is-linux-machine (not (string-equal "hyperspark")))
;	(use-package vterm :ensure t))







